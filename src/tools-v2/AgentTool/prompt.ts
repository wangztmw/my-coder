export const DESCRIPTION = `Launch a sub-agent to handle complex, multi-step tasks.
- The sub-agent gets its own context (doesn't see your conversation history).
- Use for: research, multi-file edits, running tests, exploration.
- Set run_in_background to continue working while the sub-agent runs.`;
