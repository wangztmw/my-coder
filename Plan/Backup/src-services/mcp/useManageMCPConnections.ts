/**
 * my-coder — Minimal MCP connections stub
 *
 * 原始 1,141 行 React Hook — UI 层已删除，返回空。
 */

export function useManageMCPConnections(): Record<string, unknown> {
  return { connections: [], loading: false }
}
